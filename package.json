{
  "name": "palm-las-vegas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "mkdir -p dist && cp index.html dist/index.html && cp -r assets dist/assets && cp favicon.png dist/favicon.png",
    "preview": "vite preview"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.0"
  }
}